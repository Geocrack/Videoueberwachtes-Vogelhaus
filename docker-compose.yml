services:
  web:
    image: nginx:alpine
    container_name: videoueberwachtes-vogelhaus
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
